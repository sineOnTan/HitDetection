#include <bits/stdc++.h>
#include "yawPitch.cpp"

using namespace std;

struct boundingBox {
    float x, y, z;
    float length_x, length_y, length_z;
};

bool checkBoundingBoxIntercept(cameraAngle ray, vector<boundingBox> boxes) {

}